<!--
  Ackermann-friendly navigation tree.
  Recovery behaviors keep costmap clears, wait and controlled backup,
  but intentionally remove in-place spin because the rover is non-holonomic.
  SmoothPath applied after planning for fluid trajectory following.
  IsPathValid triggers immediate replanning when the path is blocked.
  Note: BackUp recovery moves 0.20m — ensure clearance behind the robot before
  the robot attempts recovery. Nav2 BackUp already checks the costmap during
  movement and will stop on obstacle contact.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="2.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <Sequence name="ComputeAndSmooth">
              <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
              <SmoothPath path="{path}" smoother_id="savitzky_golay_smoother"/>
            </Sequence>
            <ClearEntireCostmap
              name="ClearGlobalCostmap-Context"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <ReactiveSequence name="PathFollowing">
            <IsPathValid path="{path}"/>
            <FollowPath path="{path}" controller_id="FollowPath"/>
          </ReactiveSequence>
          <ClearEntireCostmap
            name="ClearLocalCostmap-Context"
            service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearingActions">
            <ClearEntireCostmap
              name="ClearLocalCostmap-Subtree"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap
              name="ClearGlobalCostmap-Subtree"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          <Wait wait_duration="2.0"/>
          <BackUp backup_dist="0.20" backup_speed="0.12"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
